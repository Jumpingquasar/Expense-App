import 'package:flutter/material.dart';

class RemoveExpenseSnackbar extends StatelessWidget {
  const RemoveExpenseSnackbar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
